# 自适应功率限制算法学习笔记
> **适用平台**：RoboMaster M3508 / C620  
> **算法名称**：`alg_new_power_limit`

---

## 0. 为什么需要功率限制？

RoboMaster 竞赛规则对底盘电功率有严格上限（通常 80W～150W）。若不限制，PID 控制器在加速阶段的瞬时功率峰值可达上限的 **2～5 倍**，会触发裁判系统罚分甚至断电。

**核心目标：**
1. **限制总功率**：任意状态下所有电机合计消耗功率 ≤ Max_Power
2. **保持方向不变**：功率超限时等比缩减各电机输出，底盘不偏转
3. **自适应辨识**：利用 RLS 在线辨识电机损耗参数，使功率预测随时间越来越准

---

## 1. 整体流程

```
每控制周期执行一次：

Step 1  对每个电机：输入 (ω, τ_pid) → 计算理论功率 P_theory
Step 2  求和 ΣP_theory，若 ΣP ≤ Max_Power → 直接输出 pid_output，结束
Step 3  超限 → 计算收缩系数 Scale = Max_Power / ΣP_positive
Step 4  对每个消耗功率的电机：计算分配功率 P_scaled
Step 5  反解一元二次方程 → τ_limited
Step 6  τ_limited × TORQUE_TO_CMD_CURRENT → output（发往电调）
```

---

## 2. 电机功率建模

### 2.1 为什么不能用 P = ω·τ？

机械功率公式会**严重低估**实际耗电，因为忽略了电机的多种损耗，导致限制失效。

### 2.2 三项式功率模型

$$P = \omega \cdot \tau + k_1 \cdot |\omega| + k_2 \cdot \tau^2 + k_3$$

| 项 | 物理含义 | 对应损耗类型 |
|---|---|---|
| $\omega \cdot \tau$ | 转速 × 扭矩 | 有效机械功（对外做功） |
| $k_1 \cdot \|\omega\|$ | 与转速成正比 | 摩擦损耗 + 铁芯涡流损耗 |
| $k_2 \cdot \tau^2$ | 与扭矩平方成正比 | 线圈铜损 I²R（τ = Kt·I） |
| $k_3$ | 固定偏置 | 静态电流、控制逻辑等固定消耗 |

> **⚠️ 单位注意**：代码中转速单位为 rpm，功率公式中 ω 必须换算为 rad/s：
> $$\omega(\text{rad/s}) = \text{rpm} \times \frac{\pi}{30}$$

### 2.3 M3508 默认参数值

| 参数 | 默认值 | 物理量纲 |
|---|---|---|
| k1 | 0.0200 | W·s/rad（摩擦系数） |
| k2 | 485.00 | W/(N·m)²（铜损系数） |
| k3 | 11.00/8 ≈ 1.375 | W（每电机固定损耗） |

### 2.4 代码实现

```cpp
float Calculate_Theoretical_Power(float omega, float torque, uint8_t motor_index)
{
    float cmdPower = rpm2av(omega) * torque       // ① 机械功率
                   + fabs(rpm2av(omega)) * k1     // ② 摩擦/铁损
                   + torque * torque * k2         // ③ 铜损 I²R
                   + k3;                          // ④ 固定损耗
    return cmdPower;
}
```

---

## 3. RLS 自适应参数辨识

### 3.1 为什么需要自适应？

k1、k2 是经验值，会随以下因素漂移：
- 电机温升（线圈电阻随温度升高）
- 机械磨损（摩擦增大）
- 负载变化

固定参数长期运行后限制效果会变差。

### 3.2 RLS 原理

将功率方程变形，分离已知量和待辨识参数：

$$\underbrace{P_{actual} - \sum\omega\tau - 4k_3}_{\text{左边（已知）}} = k_1 \cdot \sum|\omega| + k_2 \cdot \sum\tau^2$$

这是一个**线性回归问题**，用递推最小二乘法（RLS）在线更新 k1、k2，每来一个样本就更新，**无需存储历史数据**。

```cpp
// 累加4个电机的特征量
samples[0][0] += fabsf(rpm2av(motor_data[i].feedback_omega));  // Σ|ω|
samples[1][0] += motor_data[i].feedback_torque²;               // Στ²

// RLS 更新
params = rls.update(samples, actual_power - effectivePower - 4 * k3);

// 强制参数非负（损耗不可能为负）
k1 = my_fmax(params[0][0], 1e-7f);
k2 = my_fmax(params[1][0], 1e-7f);
```

> **📌 当前状态**：RLS 框架已完备，但主流程中已注释掉，k1/k2 使用固定默认值。未来启用只需取消注释。

---

## 4. 主流程详解

### 4.1 普通四电机底盘

**Step A：计算理论功率并分类**

```cpp
for (uint8_t i = 0; i < 4; i++) {
    Motor_Data[i].theoretical_power = Calculate_Theoretical_Power(...);
    theoretical_sum += Motor_Data[i].theoretical_power;
    if (Motor_Data[i].theoretical_power > 0) {
        NeedScaled_theoretical_sum += ...;  // 只累加消耗功率（排除负功率）
    }
}
```

> **关键设计**：负功率（制动/反转时电机回馈能量）**不参与再分配**，其 output 直接透传 pid_output。

**Step B：判断是否需要再分配**

```cpp
if (Max_Power >= Theoretical_Total_Power) {
    // 未超限，直接输出 PID 结果
    for (int i = 0; i < 4; i++)
        Motor_Data[i].output = Motor_Data[i].pid_output;
    return;  // 提前退出，节省算力
} else {
    // 超限，计算统一收缩系数
    Scale_Conffient = Max_Power / NeedScaled_theoretical_sum;
}
```

例如：理论总功率 120W，上限 80W → Scale = 80/120 ≈ **0.667**，每个电机获得其理论功率的 66.7%。

**Step C：按比例分配并反解扭矩**

```cpp
for (uint8_t i = 0; i < 4; i++) {
    if (Motor_Data[i].theoretical_power < 0) {
        Motor_Data[i].output = Motor_Data[i].pid_output;  // 负功率透传
        continue;
    }
    Calulate_Power_Allocate(Motor_Data[i], Total_error, Max_Power, Scale_Conffient);
    Motor_Data[i].output = Calculate_Toque(...) * GET_TORQUE_TO_CMD_CURRENT;
    output = clamp(output, -16384, 16384);  // 限幅
}
```

### 4.2 AGV 舵轮底盘（8 电机）

AGV 有 4 个动力电机（0～3）+ 4 个转向电机（4～7），需要分组限制：

```cpp
float dir_power_limit = Max_Power * 0.8f;  // 转向电机优先分配 80%

if (theoretical_sum_dir > dir_power_limit)
    scale_dir = dir_power_limit / theoretical_sum_dir;
else
    scale_dir = 1.0f;

// 剩余功率给动力电机
mot_power_limit = Max_Power - 实际分配给转向的功率;
```

> **为何转向优先？** 若动力电机抢占过多功率，转向响应迟滞，舵轮来不及对准运动方向，导致底盘失控甚至侧翻。0.8 为经验值，可调整。

---

## 5. 核心数学：反解扭矩（一元二次方程）

### 5.1 问题描述

已知：分配功率 P_scaled、当前转速 ω
求：对应的最大允许扭矩 τ_limited

### 5.2 推导

从功率模型将 τ 视为未知量，整理为标准二次方程：

$$k_2 \cdot \tau^2 + \omega \cdot \tau + (k_1|\omega| + k_3 - P_{scaled}) = 0$$

其中 $a = k_2,\ b = \omega,\ c = k_1|\omega| + k_3 - P_{scaled}$

判别式：$\Delta = \omega^2 - 4k_2(k_1|\omega| + k_3 - P_{scaled})$

### 5.3 分支逻辑

| 情况 | 处理方式 |
|---|---|
| τ·ω ≤ 0（制动/回馈） | 直接透传原扭矩，不限制 |
| Δ > 0（正常情况） | τ>0 选较大根 solution1；τ<0 选较小根 solution2 |
| Δ ≈ 0（唯一解） | τ = -ω / (2·k2) |
| Δ < 0（配额极低） | 取抛物线顶点 τ = -ω / (2·k2) |

> **为什么选不同的根？** 两个根对应功率曲线与目标功率线的两个交点，选择与原始扭矩方向一致的根，可保持运动方向。

### 5.4 输出换算

```
output = τ_limited × TORQUE_TO_CMD_CURRENT
```

其中 `TORQUE_TO_CMD_CURRENT ≈ 52408`（转子扭矩 → CAN 控制量 ±16384）

---

## 6. 功率分配策略

### 6.1 等比收缩（当前启用）

```cpp
Motor_Data.scaled_power = Motor_Data.theoretical_power * Scale_Conffient;
```

所有电机统一乘以同一收缩系数，**运动方向不偏转**，简洁高效。

### 6.2 误差权重分配（注释中保留，暂未启用）

```cpp
if (Total_error <= ErrorLow) {
    // 误差小：等比收缩
    scaled_power = theoretical_power * Scale_Conffient;
} else if (Total_error >= ErrorUp) {
    // 误差大：按各电机误差占比分配（误差大的得更多功率）
    scaled_power = Max_Power * (Target_error / Total_error);
} else {
    // 中间段：线性插值平滑过渡
    Kp = (Total_error - ErrorLow) / (ErrorUp - ErrorLow);
    scaled_power = (1-Kp)*theoretical_power*Scale + Kp*Max_Power*(err/Σerr);
}
```

---

## 7. 关键数据结构

### 单电机数据 `Struct_Power_Motor_Data`

| 字段 | 含义 |
|---|---|
| feedback_omega | 反馈转子转速（rpm） |
| feedback_torque | 反馈转子扭矩（N·m） |
| torque | PID 输出的目标转子扭矩（N·m） |
| theoretical_power | 功率模型预测的消耗功率（W） |
| scaled_power | 再分配后的目标功率（W） |
| Target_error | PID 绝对误差（供误差权重分配用） |
| pid_output | PID 直接输出的电流控制量（±16384） |
| output | **最终发送给电调的控制量（±16384）** |

### 全局功率管理 `Struct_Power_Management`

| 字段 | 含义 |
|---|---|
| Max_Power | 裁判系统允许的最大功率（W） |
| Scale_Conffient | 本周期的功率收缩系数 |
| Theoretical_Total_Power | 所有电机理论功率之和（含负功率） |
| Needed_Scaled_Theoretical_Total_Power | 需要再分配的理论总功率（只含正功率） |
| Actual_Power | 裁判系统反馈的实际功率（供 RLS 辨识用） |

---

## 8. 重要常数与换算

| 宏/常量 | 数值 | 说明 |
|---|---|---|
| M3508_REDUATION | 3591/187 ≈ 19.20 | M3508 减速比 |
| M3508_TORQUE_CONSTANT | 0.3 | 带减速箱转矩常数（N·m/A） |
| M3508_Kt | 0.3/19.20 ≈ 0.01563 | 折算到转子的转矩常数 |
| M3508_CMD_CURRENT_TO_TORQUE | ≈ 1.908×10⁻⁵ | 控制量 → 转子扭矩（N·m） |
| M3508_TORQUE_TO_CMD_CURRENT | ≈ 52408 | 转子扭矩 → 控制量 |
| RAD_TO_RPM | 9.5493 | rad/s → rpm（即 60/2π） |

---

## 9. 边界情况处理

| 边界情况 | 处理方式 |
|---|---|
| 总功率未超限 | 直接 return，零额外计算 |
| 某电机理论功率 < 0（回馈） | 跳过限制，直接透传 |
| Δ < 0（分配功率极小） | 取抛物线顶点，保证物理合法性 |
| power ≈ 0.0f | 视为无需输出，透传原扭矩 |
| output 超出 ±16384 | 硬件限幅，防止电调保护性断电 |
| k1/k2 RLS 更新后为负 | 强制 max(value, 1e-7f) |

---

## 10. 完整调用流程（伪代码）

```cpp
// ① 初始化（上电一次）
power_limit.Init(5.0f, 100.0f);  // ErrorLow=5, ErrorUp=100

// ② 每个控制周期（1ms 定时器中断）
void TIM_PID_PeriodElapsedCallback() {
    for (int i = 0; i < 4; i++) {
        motor[i].TIM_PID_PeriodElapsedCallback();
        pm.Motor_Data[i].pid_output      = motor[i].Get_Out();
        pm.Motor_Data[i].torque          = pid_output * CMD_TO_TORQUE;
        pm.Motor_Data[i].feedback_omega  = motor[i].Get_Now_Omega_Angle();
        pm.Motor_Data[i].feedback_torque = motor[i].Get_Now_Torque();
        pm.Motor_Data[i].Target_error    = fabs(target - feedback);
    }

    pm.Max_Power    = referee.Get_Max_Power();  // 从裁判系统读取，例如 80.0f
    pm.Actual_Power = referee.Get_Power();      // 实际功率（供 RLS 用）

    // ③ 调用功率限制主函数
    power_limit.Power_Task(pm);

    // ④ 将限制后的 output 发送给电调
    for (int i = 0; i < 4; i++)
        motor[i].Set_Out(pm.Motor_Data[i].output);
}
```

---

## 11. 算法总结

**设计亮点：**

| # | 亮点 | 说明 |
|---|---|---|
| ① | 物理建模精准 | 三项式覆盖机械功率+铁损+铜损+固定损耗，远优于朴素 P=ωτ |
| ② | 等比收缩保方向 | 各电机按相同系数压缩，底盘运动方向不偏转 |
| ③ | 负功率不参与分配 | 回馈能量作为被动缓冲，符合物理直觉 |
| ④ | 一元二次反解扭矩 | 精确求解目标功率对应的允许扭矩，避免迭代 |
| ⑤ | RLS 自适应框架 | 参数辨识接口完备，未来启用只需取消注释 |
| ⑥ | AGV 分组策略 | 转向/动力分开限制，优先保证舵轮安全性 |

---

## 12. 进阶学习方向

- **深入 RLS 算法**：研究遗忘因子 λ=0.9999 的作用，参考《系统辨识》教材
- **缓冲能量控制**：研究 Buffer_K / Min_Buffer，利用电容能量进行功率平滑
- **对比分配策略**：误差权重分配 vs 等比收缩在高机动场景的优劣
- **标定实际参数**：通过不同负载下的稳态测量，标定真实的 k1/k2/k3
- **扩展至 GM6020**：完善头文件中待补充的 6020 扭矩换算宏
